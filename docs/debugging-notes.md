# Debugging Notes

Root-cause notes for meaningful bugs only (not every trivial fix).
Format: Symptom / Reproduction / Root cause / Fix / How verified.

(No entries yet — populated as real bugs are found during
implementation.)
